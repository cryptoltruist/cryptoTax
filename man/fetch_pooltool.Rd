% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_pooltool.R
\name{fetch_pooltool}
\alias{fetch_pooltool}
\title{Download Cardano staking rewards in PoolTool-compatible format}
\usage{
fetch_pooltool(
  address,
  currency = "CAD",
  save.path = NULL,
  format = FALSE,
  exchange = "exodus",
  perform = TRUE,
  list.prices = NULL,
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{address}{A Cardano stake address (\code{stake1...}) or payment/base address
(\code{addr1...}).}

\item{currency}{Local fiat currency for reward values. Currently only \code{CAD}
is supported. Defaults to \code{"CAD"}.}

\item{save.path}{Optional file path. When provided, the returned data frame is
also written to disk as a CSV file.}

\item{format}{Logical; if \code{TRUE}, return \code{\link[=format_pooltool]{format_pooltool()}} output instead of
the raw PoolTool-compatible table.}

\item{exchange}{Exchange label passed to \code{\link[=format_pooltool]{format_pooltool()}} when
\code{format = TRUE}. Defaults to \code{"exodus"} to preserve current package
behavior.}

\item{perform}{Whether to execute the API requests (if \code{TRUE}), or just
return the request objects that would be used (if \code{FALSE}).}

\item{list.prices}{Optional \code{list.prices} object to use for pricing reward
dates. Supplying this keeps the workflow deterministic and avoids implicit
online pricing lookups.}

\item{force}{Passed through to \code{\link[=match_prices]{match_prices()}} when price data must be
resolved.}

\item{verbose}{Logical; whether to print progress messages.}
}
\value{
A data frame in PoolTool generic-CSV shape, or formatted exchange
transactions when \code{format = TRUE}.
}
\description{
Downloads Cardano staking reward history for a stake or payment
address using the public Koios API and returns a data frame shaped like the
PoolTool generic CSV export. This can be passed directly to
\code{\link[=format_pooltool]{format_pooltool()}} or saved locally as a CSV file.

This helper intentionally does not scrape PoolTool. PoolTool's web UI can be
stale or change without notice, while the reward history itself is available
from chain indexer APIs. The returned columns follow the PoolTool export
layout closely enough for \code{\link[=format_pooltool]{format_pooltool()}} to consume them unchanged.
}
\examples{
fetch_pooltool(
  "stake1u88nvynpjl3lsj6wf8k7p9pxj6ta58rx2jfttqqpkumrlpsf6zeud",
  perform = FALSE
)
}
