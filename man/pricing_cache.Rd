% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing_cache_helpers.R
\name{pricing_cache}
\alias{pricing_cache}
\title{Inspect the cryptoTax pricing cache}
\usage{
pricing_cache(name = NULL, include.legacy = FALSE)
}
\arguments{
\item{name}{Optional single cache entry to inspect. One of
\code{"list.prices"}, \code{"coins.list"}, or \code{"USD2CAD.table"}.}

\item{include.legacy}{Logical; whether to also include legacy cache objects
found in \code{.GlobalEnv}.}
}
\value{
If \code{name} is \code{NULL}, a named list of cache entries. Otherwise, the
requested cached object or \code{NULL} if not present.
}
\description{
Inspect cached pricing-related objects stored by \code{cryptoTax}
during the current R session. This cache is package-owned, so it avoids
cluttering the user workspace while still keeping the shared-price workflow
easy to inspect.

The package-owned cache is a convenience layer for the current R session.
It is the only implicit cache path reused during normal pricing/FX
resolution. For reproducible or offline workflows, passing explicit
\code{list.prices}, \code{coins.list}, or \code{USD2CAD.table} inputs remains the preferred
path.

For compatibility with older workflows, \code{include.legacy = TRUE} will also
report matching objects that still live in \code{.GlobalEnv}. Those legacy
workspace objects are no longer part of the normal implicit resolution path;
they are exposed here only for inspection/transition purposes and may be
removed in a future release.
}
\examples{
pricing_cache()
pricing_cache("list.prices")
}
