% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pricing_cache_helpers.R
\name{add_to_cache}
\alias{add_to_cache}
\title{Add pricing objects to the cryptoTax cache}
\usage{
add_to_cache(list.prices = NULL, coins.list = NULL, USD2CAD.table = NULL)
}
\arguments{
\item{list.prices}{Optional \code{list.prices} object to store in the package
cache.}

\item{coins.list}{Optional output from \code{\link[crypto2:crypto_list]{crypto2::crypto_list()}} to store in
the package cache.}

\item{USD2CAD.table}{Optional USD/CAD rate table to store in the package
cache.}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
Seed the package-owned pricing cache explicitly from objects
already available in the current R session, for example after loading a
previously saved \code{list.prices} or \code{USD2CAD.table} from disk.

This is especially useful for multi-day tax workflows where you want to keep
explicit saved pricing files on disk, but still let formatter helpers reuse
those objects through the package cache during the current R session.

All supplied objects are validated before being cached. Invalid objects fail
early with a clear error instead of silently poisoning later pricing or FX
resolution.
}
\examples{
add_to_cache(list.prices = list_prices_example)
pricing_cache("list.prices")
}
