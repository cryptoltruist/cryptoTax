% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_exchanges.R
\name{format_exchanges}
\alias{format_exchanges}
\title{Format One or More Exchange Files}
\usage{
format_exchanges(data, ..., list.prices = NULL, force = FALSE)
}
\arguments{
\item{data}{A data frame, or a (possibly nested) list of exchange data
frames and/or already formatted transaction tables.}

\item{...}{Additional exchange data frames or nested lists to format.}

\item{list.prices}{A \code{list.prices} object from which to fetch coin prices.
When supplied explicitly, it must contain at least \code{currency},
\code{spot.rate2}, and \code{date2}.}

\item{force}{Whether to force recreating \code{list.prices} even though
it already exists (e.g., if you added new coins or new dates).}
}
\value{
A data frame of exchange transactions, formatted for further
processing.
}
\description{
Format one or more exchange transaction files into the
canonical transaction table used throughout \code{cryptoTax}.
This is a user-facing wrapper around \code{\link[=format_detect]{format_detect()}} for the common
workflow of passing a single exchange file, a list of raw exchange files,
or a mixed list containing already formatted transaction tables.

Empty data frames and \code{NULL} list entries are skipped. Already formatted
transaction tables are passed through unchanged.
}
\examples{
format_exchanges(data_shakepay)
format_exchanges(data_shakepay, data_newton)
format_exchanges(list(data_shakepay, data_newton))
format_exchanges(list(format_shakepay(data_shakepay), data_newton))
}
\seealso{
\code{\link[=format_detect]{format_detect()}} for the lower-level exchange auto-detection
engine, and the specific \verb{format_*()} functions for manual exchange-by-
exchange formatting.
}
